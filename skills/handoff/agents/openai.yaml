interface:
  display_name: "Handoff"
  short_description: "Preserve resumable agent session state"
  default_prompt: "Use $handoff to compact this session into a safe, resumable handoff for the next agent."

policy:
  allow_implicit_invocation: false
