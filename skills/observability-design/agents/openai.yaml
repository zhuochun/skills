interface:
  display_name: "Observability Design"
  short_description: "Design or audit operational feedback"
  default_prompt: "Use $observability-design to design the needed measurement contracts or audit whether the deployed signal-to-decision-to-action loop works."

policy:
  allow_implicit_invocation: false
