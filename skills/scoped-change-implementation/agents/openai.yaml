interface:
  display_name: "Scoped Change Implementation"
  short_description: "Implement scoped changes in coherent slices"
  default_prompt: "Use $scoped-change-implementation to implement this authorized, ready change in coherent slices, verify each step, and stop if required outcomes cannot be distinguished from available state."

policy:
  allow_implicit_invocation: true
