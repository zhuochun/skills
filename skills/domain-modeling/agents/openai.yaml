interface:
  display_name: "Domain Modeling"
  short_description: "Model behavior and semantic boundaries"
  default_prompt: "Use $domain-modeling to locate requested-surface and preserved-meaning owners, test whether modeling is warranted, compare problem-specific models, and expose consequential classification choices without assuming contexts are services."

policy:
  allow_implicit_invocation: false
