interface:
  display_name: "Service Boundary Design"
  short_description: "Choose service versus module enforcement"
  default_prompt: "Use $service-boundary-design to decide whether this boundary needs an in-process module, a deployable service change, or stronger data and runtime enforcement."

policy:
  allow_implicit_invocation: false
