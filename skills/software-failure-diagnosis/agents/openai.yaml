interface:
  display_name: "Software Failure Diagnosis"
  short_description: "Diagnose failures with causal evidence"
  default_prompt: "Use $software-failure-diagnosis to establish the supported cause of this failure before any authorized repair."

policy:
  allow_implicit_invocation: true
