interface:
  display_name: "Agentic Repo Setup"
  short_description: "Set up repositories for reliable coding-agent work"
  default_prompt: "Use $agentic-repo-setup to bootstrap or retrofit this repository around accepted constraints. Start from effective repo AGENTS instructions, route each claim to its accountable authority, establish stable commands with explicit focused and full evidence states, and add only proportional controls."

policy:
  allow_implicit_invocation: false
