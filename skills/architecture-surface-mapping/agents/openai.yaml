interface:
  display_name: "Architecture Surface Mapping"
  short_description: "Map architecture surfaces and journeys safely"
  default_prompt: "Use $architecture-surface-mapping to orient me to the critical architecture surfaces or trace one bounded end-to-end journey, then identify contradictions and the next evidence to gather."

policy:
  allow_implicit_invocation: true
