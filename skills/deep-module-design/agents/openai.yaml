interface:
  display_name: "Deep Module Design"
  short_description: "Design or retain modules around real change"
  default_prompt: "Use $deep-module-design to compare the current boundary with alternatives and decide whether to retain it or adopt a deep or composable interface for this demonstrated change pressure."

policy:
  allow_implicit_invocation: false
