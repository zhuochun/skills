interface:
  display_name: "Deep Module Design"
  short_description: "Decide module and interface boundaries"
  default_prompt: "Use $deep-module-design to decide whether the current module boundary should remain or change before implementation."

policy:
  allow_implicit_invocation: true
