interface:
  display_name: "Behavior-Preserving Refactoring"
  short_description: "Refactor structure while preserving behavior"
  default_prompt: "Use $behavior-preserving-refactoring to classify the requested effect, route semantic changes out, and improve structure without changing supported behavior."

policy:
  allow_implicit_invocation: true
